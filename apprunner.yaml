version: 1.0

runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
  env:
    - name: ENVIRONMENT
      value: production

run:
  command: python app.py
  env:
    - name: PORT
      value: "8080"
